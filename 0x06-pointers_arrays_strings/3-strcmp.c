#include <string.h>

/**
 * _strcmp - Compares two string s1 and s2
 * Return the difference between the ASCII of
 * the first non matching character in s1 and s2
 * @s1: first string
 * @s2: second string
 *
 * Return: integer
 */

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
